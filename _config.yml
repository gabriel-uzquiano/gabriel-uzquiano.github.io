title: "gabriel uzquiano"
description: "A portal with links to course materials."
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://gabriel-uzquiano.github.io" # the base hostname & protocol for your site
theme: minima
plugins:
  - jekyll-feed
  - jekyll-seo-tag

# Navigation links
# Add navigation links to your site using the `nav` key.
# For external links add the `external: true` flag.
nav:
  - title: "Home"
    url: "/"
  - title: "Courses"
    url: "/courses/"

# Build settings
markdown: kramdown
kramdown:
  math_engine: mathjax
  math_engine_opts:
    preview: false
highlighter: rouge
